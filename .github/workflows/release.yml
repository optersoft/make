name: Release

# Publishes to PyPI through Trusted Publishing (OIDC), so no API token exists
# anywhere -- not in repository secrets, not on a laptop. Configure the
# publisher once at https://pypi.org/manage/project/mkrun/settings/publishing/
# with: owner `optersoft`, repository `make`, workflow `release.yml`,
# environment `pypi`.
on:
  push:
    tags: ["v*"]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
      # --all-packages reaches the recipes/ workspace member. pytest collects its
      # suite too, so without it the verify step below fails on import rather
      # than on anything about the release.
      - run: uv sync --all-extras --all-packages
      - name: Verify before shipping
        run: |
          uv run ruff check src tests Makefile.py recipes/src recipes/tests recipes/examples
          uv run ruff format --check src tests Makefile.py recipes/src recipes/tests recipes/examples
          uv run pytest
      - name: Check the tag matches the version
        run: |
          tag="${GITHUB_REF_NAME#v}"
          version="$(uv run python -c 'import make; print(make.__version__)')"
          if [ "$tag" != "$version" ]; then
            echo "tag v$tag does not match __version__ $version" >&2
            exit 1
          fi
        if: startsWith(github.ref, 'refs/tags/v')
      # Named explicitly: this repository is a workspace whose other member is a
      # private recipe package, and dist/ is uploaded to PyPI wholesale below.
      # A bare `uv build` builds the root today, but nothing in that sentence is
      # a guarantee worth resting the upload on.
      - run: uv build --package mkrun
      # The sdist must stay the tool alone: it defaulted to the whole project
      # directory once and swept in the recipe package's sources -- which now
      # live in this repository, at recipes/, so this check is the thing
      # standing between a release and publishing them.
      - name: The sdist contains only the tool
        run: |
          unexpected="$(tar tzf dist/*.tar.gz | sed 's|^[^/]*/||' \
            | grep -vE '^(src/make/|tests/|README.md|LICENSE|pyproject.toml|PKG-INFO|\.gitignore|$)' || true)"
          if [ -n "$unexpected" ]; then
            echo "unexpected files in the sdist:" >&2; echo "$unexpected" >&2; exit 1
          fi
      - uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/

  publish:
    needs: build
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      id-token: write        # the OIDC token Trusted Publishing exchanges
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist/
      - uses: pypa/gh-action-pypi-publish@release/v1
